name: ostorlab_insecure_flutter_app
description: Flutter module for Ostorlab Insecure Android app.

version: 1.0.0+1

environment:
  sdk: '>=2.19.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  encrypt: ^5.0.1
  agent_dart: 1.0.0-dev.28         # vulnerable version
 
  crypto: ^3.0.3
  webview_flutter: ^4.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
